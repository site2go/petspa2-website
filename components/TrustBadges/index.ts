/**
 * Trust Badges Component
 *
 * Visual credibility signals for conversion optimization
 *
 * Features:
 * - Pre-built badge types (rating, guarantee, certification, etc.)
 * - Horizontal scroll on mobile
 * - Optional auto-scroll marquee animation
 * - Layout-aware spacing and styling
 *
 * Badge icon options:
 * - Star (ratings)
 * - Shield (guarantees)
 * - Award (certifications)
 * - Users (customer count)
 * - Clock (years experience)
 * - CheckCircle (verified)
 */

export {
  TrustBadges,
  trustBadgePresets,
  type TrustBadge,
} from "./TrustBadges";

export { TrustBadges as default } from "./TrustBadges";
